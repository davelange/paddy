{
  "name": "paddy",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run server.ts",
    "dev": "bun run --hot server.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.12",
    "@types/qrcode-terminal": "^0.12.2"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "qrcode-terminal": "^0.12.0"
  }
}
